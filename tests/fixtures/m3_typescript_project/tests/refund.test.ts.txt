import assert from "node:assert/strict";
import test from "node:test";

import { calculateRefund } from "../src/refund.js";

test("calculateRefund subtracts the fee", () => {
  assert.equal(calculateRefund(100, 5), 95);
});
